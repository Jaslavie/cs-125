import SwiftUI

// MARK: - Preferences View

/* Placeholder for future editing of user preferences (e.g. walking radius, budget, price sensitivity, distance acceptance, typical stay). 
 * Deferred in the initial frontend so the main search flow and ranked results UI could be built first.
 *
 * Attributes: none; body is a placeholder "Preferences" label.
 */
struct PreferencesView: View {
    var body: some View {
        Text("Preferences")
    }
}
